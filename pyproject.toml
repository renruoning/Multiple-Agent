[project]
name = "biz-travel-agents"
version = "0.1.0"
description = "智能商旅多智能体框架（LangGraph 实现）"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
