# 选择 LLM 提供方: anthropic 或 openai
LLM_PROVIDER=anthropic

# 对应提供方的模型名（留空则使用代码内默认值）
LLM_MODEL=claude-sonnet-5

# 采样温度
LLM_TEMPERATURE=0.2

# 二选一（或都填，按 LLM_PROVIDER 使用对应的那个）
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
